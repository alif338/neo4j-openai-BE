export interface ExampleInterface {
    id: number;
    title: string;
    completed: boolean;
}

export interface User {
    name: string;
    age: number;
}

export interface Browse {
    search: string;
    limit: number;
    page: number;
}